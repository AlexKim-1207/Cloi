docs/SESSION_12_PROMPT.md + 갱신된 CLAUDE.md '🚀 배포 검증 규칙' 섹션 정독.

★ 중요 변경사항: server/src/worker.ts 수정 시 반드시 'npm run build && npx wrangler pages deploy dist --project-name=cloi' 도 실행. cloi-api Worker 단독 deploy 는 효과 없음 (functions/api/[[route]].ts 가 worker.ts 를 import 해서 Pages가 진짜 production).

작업 순서 (overnight 모드 — 자동 실행):

STEP 0. bash scripts/verify_deploy.sh fashion-search/eval/queries/q001.jpg 실행. exit 0 안 나오면 진단 후 Pages 재배포 (npm run build + wrangler pages deploy dist) 후 다시 verify.

Track A. (skip — 이미 overnight 스크립트 STEP 1 에서 배포됨, verify 통과면 OK)

Track B. server/src/worker.ts 의 FASHION_PROMPT 에 outfit-level 추론 메타 추가:
  - 응답 root 에 gender, gender_confidence, gender_signals, price_tier, price_tier_confidence, price_signals, price_range_estimate, season, vibe 추가
  - SESSION_12_PROMPT.md Fix 12B-1 정확히 따라 수정
  - 수정 후 npm run build + wrangler pages deploy dist
  - verify_deploy.sh 재실행해서 응답에 'gender' 필드 등장 확인

Track C. softScoreProducts 함수 추가 + /api/search/categories 핸들러에 적용:
  - SESSION_12_PROMPT.md Fix 12C-1 ~ 12C-3 정확히 따라
  - 성별/가격/색상 모두 confidence-weighted soft multiplier (hard reject 절대 X)
  - 수정 후 npm run build + wrangler pages deploy dist + verify

Track D/E/F. (overnight 안전성 — SKIP). UX 변경/GitHub Actions/Cloud Run min-instances 변경 등 사용자 승인 필요한 작업은 모두 SKIP. 다음 세션에서 사용자와 함께.

검증:
  - 5개 라이브 테스트 (T1 여성 셀카, T2 남성 outfit, T3 럭셔리 lookbook, T4 단일 가방, T5 캐주얼) 자동 실행
  - 각 케이스마다 _source 필드 + gender 필드 + 1순위 상품 (제목+가격) 응답 받아서 logs/test_results.json 에 저장

종료:
  - 모든 git commit 완료
  - SESSION_STATUS.md 정직하게 업데이트 (verify 통과한 것만 '완료', 실패한 것은 '시도 후 검증 실패'로)
  - git push origin main
  - logs/overnight_summary.md 작성 (이번 세션 결과 요약, 다음 세션 우선순위)

본질 원칙 두 개 절대 잊지 말 것:
  1. '검열이 아니라 추론. hard exclude/cap 절대 금지. 모든 신호는 soft score multiplier'
  2. 'deploy 명령 실행 ≠ production 적용. wrangler pages deploy dist + verify_deploy.sh exit 0 만이 진짜 완료. cloi-api 단독 deploy 는 효과 없음.'

위험 발견 시 (예: 코드 컴파일 에러 반복, Gemini API 503 5회 이상, Cloud Run 인증 만료) — 작업 멈추고 logs/overnight_log.txt 에 상태 기록 후 깨끗하게 종료. 절대 무한 루프 X.
