docs/SESSION_14_PROMPT.md + uploads/deep-research-report (1).md + 갱신된 CLAUDE.md '🚀 배포 검증 규칙' 정독.

★ 본질 원칙:
"코드 작성 ≠ production 적용. SESSION 14는 코드 작성만 끝나고 Cloud Run deploy 안 함. 라이브 회귀까지 발생. SESSION 15는 진짜 production 적용 + 라이브 검증 의무."

★ 배포 path 기억:
- 사용자 트래픽: cloi.pages.dev/api/* → functions/api/[[route]].ts → server/src/worker.ts (Worker 측)
- v3 path: Worker → Cloud Run /api/search → fashion-search/apps/api/routes_search.py + ML 처리 (Cloud Run 측)
- worker.ts 수정 시: cd server && npm run build && cd .. && npx wrangler pages deploy dist --project-name=cloi
- fashion-search/ 수정 시: bash fashion-search/deploy.sh (★ SESSION 14에서 빠진 게 이 단계)
- 모든 deploy 후 bash scripts/verify_deploy.sh 통과 의무

★ SESSION 14 회귀 (이번 세션 즉시 fix):
- Worker 측 FASHION_PROMPT 길어져 Gemini가 마크다운 응답 → JSON parse 실패 → 500 에러
- worker.ts 에 responseMimeType: "application/json" 이미 코드 fix 됨 (commit 안 됨)
- Pages 재배포로 적용 + verify

★ SESSION 14 미배포 (이번 세션 진짜 적용):
- fashion-search/src/preprocess/ 4개 파일 (garment_detect.py, text_mask.py, face_mask.py, pipeline.py)
- fashion-search/src/retrieval/ 2개 파일 (feature_builder.py, ranker.py)
- fashion-search/src/pricing/ 2개 파일 (sku_matcher.py, effective_price.py)
- fashion-search/eval/ 3개 파일 (regression_test.py, benchmark_models.py, gold_set/README.md)
- requirements.txt: transformers, paddleocr, paddlepaddle, xgboost, open_clip_torch 추가됨
- 모두 git commit 됨, 단 Cloud Run 재배포 안 됨

작업 순서 (overnight 자율 실행):

═══════════════════════════════════════════════════════════
STEP 0. Pre-check
═══════════════════════════════════════════════════════════

bash scripts/verify_deploy.sh fashion-search/eval/queries/q010.jpg
# 현재 회귀 상태 확인 (500 에러 예상)
# baseline 기록 후 STEP 1 진행

═══════════════════════════════════════════════════════════
STEP 1. Hotfix 검증 + Pages 재배포 (Worker 회귀 차단)
═══════════════════════════════════════════════════════════

1-1. server/src/worker.ts 의 generationConfig 에 responseMimeType: "application/json" 있는지 확인 + FASHION_PROMPT 마지막에 "★ 출력 강제: 반드시 JSON 객체 하나로만 응답" 있는지 확인.
없으면 추가.

1-2. Pages 재배포:
cd server && npm run build && cd ..
npx wrangler pages deploy dist --project-name=cloi
sleep 30

1-3. 즉시 검증:
bash scripts/verify_deploy.sh fashion-search/eval/queries/q010.jpg
- _source 응답 OK
- 500 에러 사라짐
- gender, price_tier, pattern, length 필드 응답

git add -A && git commit -m "hotfix(worker): JSON response mime type + prompt hardening (SESSION 14 회귀 fix)"

═══════════════════════════════════════════════════════════
STEP 2. ★ Cloud Run 진짜 재배포 (SESSION 14의 본질) ★
═══════════════════════════════════════════════════════════

2-1. fashion-search/Dockerfile 검증:
- requirements.txt에 추가된 의존성 (transformers, paddleocr, paddlepaddle, xgboost, open_clip_torch) 모두 빌드 가능한지 확인
- paddleocr는 무거우니 (~500MB) Docker image 크기 증가 예상
- 만약 Docker base image가 너무 작으면 system dependencies (libgl1, libglib2.0-0 등) 추가 필요

2-2. requirements.txt 안전성 체크:
- paddlepaddle은 PyPI에 wheel 있음 (CPU 버전)
- transformers + torch 호환 버전 확인
- Grounding DINO model 다운로드 (~700MB) — Cloud Run startup 시 cache 필요

2-3. fashion-search/src/preprocess/pipeline.py 가 routes_search.py에서 실제 호출되는지 확인:
- routes_search.py 가 _build_per_tab_query_embs 사용 중인지
- 또는 새 preprocess.pipeline.preprocess_image 호출 추가됐는지
- 만약 통합 안 됐으면 routes_search.py 수정 (preprocess 결과를 garment_crops 로 넘김)

2-4. Cloud Run deploy 시도:
bash fashion-search/deploy.sh
# 빌드 시간 5~15분 예상 (큰 의존성)
# 실패 가능 원인:
#   - Docker base image 작아서 system lib 부족 → Dockerfile에 apt-get install 추가
#   - paddleocr import 시 추가 의존성 필요 → requirements.txt 보강
#   - 이미지 크기 초과 (Cloud Run 32GB 제한, 우리는 그 안일 것)
#   - GCP 빌드 timeout (기본 10분, --timeout=20m 추가)
# 성공: "Deployed [revision] to [URL]" 메시지

2-5. Cloud Run /health 검증:
curl https://fashion-search-dibvogjuma-du.a.run.app/health
# 기대: {"status":"ok","version":"3.0.0",...}
# 새 의존성 import 에러 시 503 또는 500 응답 → 로그 확인 후 fix

2-6. Cloud Run /api/search 직접 호출 (timeout 120s):
curl -X POST https://fashion-search-dibvogjuma-du.a.run.app/api/search \
  -F "file=@fashion-search/eval/queries/q010.jpg" \
  --max-time 120 -o cloud_run_resp.json
# 응답 확인:
#   - HTTP 200
#   - JSON parse 가능
#   - tabs 배열 존재 (v3 응답 구조)
#   - 처리 시간 < 90s (Worker timeout)
# 처리 시간 90s 초과 시:
#   - rembg / paddleocr 등 무거운 단계 비활성화 옵션 추가
#   - 또는 Worker timeout 120s로 추가 연장

git add -A && git commit -m "feat(cloud-run): Track A 14A 통합 + deploy (Grounding DINO + PaddleOCR + face mask 진짜 적용)"

═══════════════════════════════════════════════════════════
STEP 3. v3 Path 적중률 측정 (SESSION 14 진짜 효과 검증)
═══════════════════════════════════════════════════════════

bash scripts/measure_v3_hit_rate.sh
# 10회 호출 후 _source 분포
# 목표: v3 적중률 50%+ (Cloud Run 처리 90s 안에 끝나면 적중)
# 만약 0% 적중:
#   - Cloud Run 처리 시간 측정 (curl --write-out)
#   - 90s 초과 단계 식별
#   - 무거운 단계 비활성화 또는 Worker timeout 연장

═══════════════════════════════════════════════════════════
STEP 4. 라이브 5케이스 검증 (사용자 호소 직접)
═══════════════════════════════════════════════════════════

5케이스 자동 호출 + 응답 분석:
1. fashion-search/eval/queries/q001.jpg (베이지 터틀넥)
   기대: top_inner color="베이지", _source="v3" or "worker_gemini"
2. q010.jpg (룩북) — 멀티 카테고리 detect
   기대: cats_count >= 4
3. q020.jpg (가방)
   기대: bag detect + 다양한 가격대
4. q030.jpg (캐주얼)
5. q040.jpg (다른 스타일)

각 케이스 응답:
- _source
- gender + gender_confidence
- price_tier + price_range
- 카테고리 개수
- top1 result (제목 + 가격)

logs/test_results_s15.json 저장.

═══════════════════════════════════════════════════════════
STEP 5. SESSION_STATUS.md 정직 업데이트
═══════════════════════════════════════════════════════════

진짜 production 적용 여부를 verify_deploy.sh + measure_v3_hit_rate.sh 결과 기반으로 정직 기록:

- "✅ Track A Cloud Run 적용 + verify 통과 + v3 적중률 X%" (성공)
- "⚠ Track A Cloud Run deploy 시도 후 빌드 실패 (사유: ...)" (실패)
- "⚠ Cloud Run deploy 성공했으나 v3 적중률 0% (Cloud Run 처리 시간 100s 초과)" (부분 실패)

logs/session15_summary.md 작성:
- Cloud Run deploy 성공 여부
- v3 적중률 (목표 50%+ 달성?)
- 라이브 5케이스 결과
- 잔여 작업 (Track B/D/E 학습 데이터 등)

═══════════════════════════════════════════════════════════
STEP 6. git push + 알람
═══════════════════════════════════════════════════════════

git push origin main
.claude/session-done.ps1

═══════════════════════════════════════════════════════════
위험 발견 시 행동
═══════════════════════════════════════════════════════════

다음 상황 시 즉시 작업 멈추고 logs/overnight_log 에 기록 후 깨끗하게 종료:

- Cloud Run 빌드 3회 연속 실패 (의존성 문제, Docker 빌드 에러)
  → SESSION_STATUS.md에 "Track A Cloud Run 의존성 충돌. paddleocr 대안 (EasyOCR) 또는 단계 분리 검토 필요" 기록
- gcloud 인증 만료
  → "사용자 직접 'gcloud auth login' 후 재시도 필요" 안내
- Cloud Run /health 503 반복 (코드 import 에러)
  → routes_search.py 수정 commit 후 재배포 시도. 3회 연속 실패 시 import 라인 주석처리 + scaffold 만 commit
- Cloud Run /api/search 처리 시간 200s+ (Worker timeout 120s로도 부족)
  → "Cloud Run CPU에서 처리 한계. Modal Labs 등 GPU serverless 검토 필요" 기록
- Pages 재배포는 됐는데 verify_deploy.sh 가 실패 (회귀)
  → 즉시 git revert HEAD~1 + 재배포 + verify

본질 원칙 두 개:
1. '코드 작성 ≠ production 적용. Cloud Run deploy + curl /api/search 응답 확인 만이 진짜 완료.'
2. 'SESSION 14 회귀 (Gemini 마크다운 응답) 같은 사고 방지 — 모든 prompt 변경 후 verify_deploy.sh 통과 + 라이브 5케이스 호출 + JSON parse 성공 확인 의무.'
